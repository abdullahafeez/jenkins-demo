pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building the packages'
            }
        }
      stage('Test') {
            steps {
                echo 'Testing the app....'
            }
        }
      stage('Deploying') {
            steps {
                echo 'Deploying the app...'
            }
        }
    }
}
